<h1>投稿詳細</h1>
<p>【メールアドレス】 <%= @food.user.email %></p>
<p>【料理名】 <%= @food.name %></p>
<%= render "like", food: @food %>
<% if @food.avatar.attached? %>
  <div>
    <%= image_tag @food.avatar %>
  </div>
<% end %>
<p>【コメント】 <%= simple_format(h(@food.comment)) %></p>
<% if current_user.id == @food.user_id %>
  <p>
    <%= link_to "編集", edit_food_path(@food) %>
    <%= link_to "削除", food_path(@food), data: { turbo_method: :delete, turbo_confirm: "削除してよろしいですか？" } %>
  </p>
<% end %>
<p><%= link_to "投稿一覧へ", foods_path %></p>